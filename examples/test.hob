type WrappedI32 = i32;

type Test = struct {
    a: i32,
    b: struct {
        c: WrappedI32,
    },
};

extern(putchar) fun putCharacter(c: u8);

return
    0
       ;

global fun main() -> i3233 {
    putCharacter(72);
    putCharacter(101);
    putCharacter(108);
    putCharacter(108);
    putCharacter(111);
    putCharacter(44);
    putCharacter(32);
    putCharacter(87);
    putCharacter(111);
    return 0;
    putCharacter(114);
    fun test() {}
    putCharacter(108);
    putCharacter(100);
    putCharacter(10);
    return 0;
}
