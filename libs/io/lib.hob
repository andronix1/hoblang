import std;

public type Fd = i32;

extern fun Fd.write(self, ptr: *u8, size: std.def.Size);

public fun Fd.putChar(self, char: u8) {
    self.write(&char, 1);
}

public fun out() -> Fd {
    return 1;
}
