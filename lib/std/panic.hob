import "str.hob" as str;

use str.string;

public var handler: fun (string) = fun (message) {
    while true {}
};

public fun assert(ok: bool, message: string) {
    if (!ok) {
        handler(message);
    }
}
